class Ticket {
    constructor(props) {
        this.id = props.id
        this.code = props.code
        this.created_at = props.created_at
        this.amount = props.amount
        this.pursacher = props.pursacher
    }
}

export default Ticket;